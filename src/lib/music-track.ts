/** The background soundtrack file.
 *  Set MUSIC_URL to the uploaded track's CDN url. While it is null the app
 *  simply plays no music and every control below stays harmless. */
export const MUSIC_URL: string | null = null;
